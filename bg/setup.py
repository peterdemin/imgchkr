from setuptools import find_packages, setup

setup(
    name='imgchkr_bg',
    version='0.0.0',
    package_dir={'': 'src'},
    packages=find_packages('src'),
    # install_requires=['imgchkr_lib'],
)
